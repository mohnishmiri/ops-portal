# Guide Section 10: Legend Reuse After Azure

EV-DOCX-011; blocks 33-37. Block 35 repeats the N/R arrow-color instruction; block 37 references the same image2.png used earlier. The image hash is unchanged, so prior extracted media and qualified local OCR were reused without repeating OCR.

- VERIFIED: the guide reuses one legend image across the interface regions.
- No new inclusion or direction rule appears in this fragment. The repeated legend should not become a separate per-region color implementation.
- OCR remains limited to the legend title and Multiple Protocols label. Exact color/direction rows still need approved-policy and visual verification.
- Reuse one versioned legend/style contract while keeping each region's category selector explicit. Resolve any prose/legend discrepancy against existing authorized policy before proposing a new decision.
- This fragment creates no new application or deployed-resource facts. Source/reference files remain unchanged.