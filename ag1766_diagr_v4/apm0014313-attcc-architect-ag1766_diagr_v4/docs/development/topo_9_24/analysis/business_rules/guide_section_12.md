# Guide Section 12: Standard Box With Conditional Annotation

EV-DOCX-013; blocks 40-45 and image11.png. Block 41 calls the box standard across topology diagrams. Local OCR recovers `Email`, `WINDOW s`, and `ONLY`, but not their precise spatial association or all other image labels.

- VERIFIED prose: reusable box structure is intended.
- PARTIALLY VERIFIED image evidence: a Windows-only annotation appears in the transcription. It is not established which pictured element it qualifies; do not assume it qualifies Email or every item in the box.
- Proposed interpretation boundary: standard layout can contain conditionally applicable content. Any OS-dependent item needs an explicit approved condition evaluated from existing reviewed facts, not an unconditional default.
- Keep uncertain applicability visible. Unknown OS or missing facts must not be converted into a positive or negative application capability.
- Correlate this annotation to the actual base/profile and approved application target before implementing a conditional rule. No new rule has been approved here.
- Original files remain unchanged. Image transcription is local and not a replacement for final visual confirmation.