# Guide Section 09: Azure Interfaces

EV-DOCX-010; guide blocks 28-32 and image9.png.

## Verified Prose

- The region is for Azure interfaces.
- Column G selects the Azure category.
- The text specifies inbound connectivity and grouping by columns N and R.
- Arrow color coding uses the same N/R grouping.

Local OCR suggests repeated application-name/acronym placeholders. Exact identifiers and connection geometry remain unverified; illustrative values are not migration facts.

## Topology Rule Implications

- This is a variable external-interface region, not a request for live Azure inventory collection or a fixed set of cloud resources.
- Consume reviewed canonical equivalents of category, scoped counterparty identity, direction, protocol/grouping and provenance. Do not inspect/reparse the intake workbook at render time.
- Repeated application placeholders support a requirement to evaluate repeated-node/flow capability against the application-specific expected target. Exact multiplicity must come from reviewed facts, not placeholder counts.
- Preserve Azure as a category distinct from AWS, internal/on-prem categories, environment, account, region and site.
- Share one approved legend policy with the other interface regions. The direction/legend ambiguity remains an explicit policy question, not permission to choose an interpretation silently.
- Proposed acceptance: Azure-category facts populate only the approved Azure region, with scoped deduplication and correct reviewed direction/style; unknown required fields become typed gaps. No tests were run.

Source and image hashes remained unchanged. OCR was local and is not visual certification.