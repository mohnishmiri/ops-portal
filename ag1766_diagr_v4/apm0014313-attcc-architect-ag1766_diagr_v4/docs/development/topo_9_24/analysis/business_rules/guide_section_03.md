# Guide Section 03: AWS Tier 1

EV-DOCX-004; S06-C04. Guide blocks 8-13 and embedded image4.png were extracted without changing the DOCX. Image text was transcribed by local Windows OCR; layout and connector interpretation remain visually unverified after the response-image retrieval problem.

## Verified Prose Rules

- Block 8 identifies the AWS Tier 1 box and refers to the Interface sheet.
- Block 9 selects interfaces whose column G category is AWS, states inbound connectivity, and groups by columns N and R.
- Block 10 assigns arrow color coding based on the same N/R grouping.
- These are topology-consumption requirements. Intake parsing/import correctness remains outside this review; consume reviewed canonical equivalents of these fields.

## Qualified Image Evidence

The OCR recognizes an AWS Tier 1/PaaS-style heading, AWS service/tooling labels, and an example application box. The full transcription stays in evidence/docx/derived/image4-ocr.json; the example application's identity is not repeated here or promoted to this migration's facts.

The illustration alone does not require every depicted AWS service for every application. The prose selector, reviewed facts, approved standard profile content, and the specific user-confirmed target must determine inclusion. OCR does not establish edge direction or the relation of nearby labels to particular connectors.

## Proposed Projection/Rendering Contract

- Select by an explicitly normalized AWS interface category, without confusing that category with environment, region, account, or site.
- Preserve scoped counterparty identity, provenance, direction and the reviewed N/R grouping fields.
- Render repeated endpoints/flows only inside the approved AWS Tier 1 region; retain standard profile content separately from application-derived nodes.
- Resolve the shared direction/legend ambiguity identified in F-RULE-001 through the existing approved policy or an authorized decision, not guessed arrow geometry.
- Proposed acceptance: relevant AWS-category interfaces appear exactly once per approved scoped identity/flow; other categories do not leak into this region; unknown required fields remain explicit gaps. These checks are not executed suite results.

## Uncertainties

Exact N/R semantics and color mapping, page/region selectors, image connector details, and which illustrated items are standard versus application-derived need later policy/profile comparison. No image label has been adopted as a confirmed deployed resource merely because it appears in the guide.