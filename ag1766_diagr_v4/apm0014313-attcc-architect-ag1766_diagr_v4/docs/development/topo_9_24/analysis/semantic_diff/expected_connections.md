# Candidate Expected-Output Connections

EV-DRAWIO-006; S04-C03. Two pages, 139 edges total. The safe parser verified unchanged source bytes and saved redacted, opaque endpoint/arrow/point records.

## Verified Structural Facts

- 64 edges omit a source attribute; 83 omit a target attribute. These overlapping counts are not missing-relationship totals.
- Zero named source/target references are dangling after per-page wrapper identity resolution.
- 101 edges contain explicit sourcePoint geometry; 118 contain explicit targetPoint geometry.
- Business direction remains NOT_INFERRED. Arrow decoration alone does not establish inbound/outbound integration semantics.

## Comparison Boundary

- Input: one page, 98 vertices, 41 edges. Candidate expected output: two pages, 363 vertices, 139 edges in total.
- This is a verified difference in document structure, not yet an approved list of resources/flows the generator must add. Per-page scope and object correspondence remain unresolved.
- Both documents include geometry-anchored/manual-style connectors; a semantic extractor must preserve uncertainty instead of treating every absent endpoint attribute as a lost relationship.
- Stage 04 structural inventory/hierarchy/connections is complete. Semantic comparison remains qualified until the candidate output's intended role/application/scope is established.

Next: confirm whether the supplied output is the target to reproduce or an illustrative reference. The intake XLSX source also remains unspecified. Do not search excluded folders or silently choose private evidence to fill these gaps.