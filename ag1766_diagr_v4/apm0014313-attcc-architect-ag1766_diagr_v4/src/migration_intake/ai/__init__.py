"""
AI candidate-mapping layer for Migration Intake.

This package provides the CandidateMapper protocol, Pydantic v2 models
for mapping requests and results, and a deterministic mock implementation
for tests and offline use.

Modules:
- models: MappingRequest, MappingResult, ProposedMapping data models
- port: CandidateMapper Protocol definition
- mock: MockCandidateMapper deterministic implementation
"""
