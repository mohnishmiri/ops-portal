# Superseded: Oracle Architecture Diagram

This document has been consolidated into the authoritative execution plan:

- `ORACLE_INTEGRATION_DESIGN.md`

Use that document for the target dependency flow, abstraction policy, execution dependency graph, ownership matrix, and acceptance gates. This pointer remains temporarily to prevent stale links from directing agents to conflicting guidance.
