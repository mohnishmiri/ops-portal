# Superseded: Oracle Abstraction Strategy

This document has been consolidated into the authoritative execution plan:

- `ORACLE_INTEGRATION_DESIGN.md`

Use that document for architecture boundaries, TDD packets, parallel-agent ownership, gates, verification, data cutover, and enterprise certification. This pointer remains temporarily to prevent stale links from directing agents to conflicting guidance.
