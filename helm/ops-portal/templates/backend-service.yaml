apiVersion: v1
kind: Service
metadata:
  name: {{ include "ops-portal.fullname" . }}-backend
  labels:
    app.kubernetes.io/name: {{ include "ops-portal.name" . }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ops-portal.name" . }}
    app.kubernetes.io/component: backend
